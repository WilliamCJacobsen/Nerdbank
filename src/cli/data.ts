/*interface CustomerDTO {
  age: number;
  name: string;
  ssnKey: string;
  lastName: string;
  firstName: string;
  customerKey: string;
  organisationNumber: string;
}

interface LinksDTO {
  // Define the properties of LinksDTO here.
}

interface AccountPropertiesDTO {
  // Define the properties of AccountPropertiesDTO here.
}

interface Account {
  key: string;
  iban: string;
  name: string;
  type: string;
  owner: CustomerDTO;
  _links: LinksDTO;
  balance: number;
  linksDTO: LinksDTO;
  obsCodes: any[]; // Replace 'any' with the actual type if known
  productId: string;
  description: string;
  productType: string;
  currencyCode: string;
  disposalRole: boolean;
  accountNumber: string;
  descriptionCode: string;
  availableBalance: number;
  accountProperties: AccountPropertiesDTO;
  creditCardAccountID: string;
  creditCardCreditLimit: number;
  eInvoiceCustomerReference: string;
}

interface AccountDTO {
  _links: {};
  errors: string[];
  accounts: Account[];
}
*/
